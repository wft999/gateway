CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(LMdatabase)

AUX_SOURCE_DIRECTORY(. SRC)
INCLUDE_DIRECTORIES(${TD_ROOT_DIR}/src/edb3/inc)
ADD_EXECUTABLE(edb2demo main.c)

TARGET_LINK_LIBRARIES(edb2demo  edb)   
